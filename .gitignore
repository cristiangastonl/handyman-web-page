node_modules
dist
.env
.env.local
.env.supabase
.vercel
.mcp.json
.DS_Store
*.pdf
test-*.mjs

# harness
test-results
playwright-report
blob-report
.playwright

# contiene tokens de acceso — nunca commitear
.claude/settings.local.json

# fotos que trae el cliente para subir a Supabase — nunca al repo
_incoming/

# Copia local de Supabase (1.6 GB de fotos): nunca al repo.
# Se regenera con: npm run backup
backup/
